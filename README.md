
# Perintah
    $ pkg update && pkg upgrade
    $ pkg install python
    $ pkg install git
    $ pip install requests
    $ pip install futures
    $ git clone https://github.com/mahlepa277/ig2
    $ cd ig2
    $ git pull
    $ python cot.py
